<!DOCTYPE html>
<html>
  <head>
    <title>Eventbrite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css' %>
    <script src="https://kit.fontawesome.com/990e8738f9.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-pink fixed-top">
      <div class="container-fluid">
      <a href="<%= root_path %>" class="navbar-brand d-flex align-items-center">
        <span class="ml-2 fs-4 logo-brand">Eventbrite</span>
      </a>    
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a href="<%= new_event_path %>" class="nav-link text-white">Lancer un évènement</a>
            </li>
            <% if current_user %>
              <li class="nav-item">
                <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "nav-link text-white", data: { method: :delete, confirm: "Êtes-vous sûr de vouloir vous déconnecter ?" } %>
              </li>
              <li class="nav-item">
                <%= link_to user_path(current_user.id), class: "nav-link text-white ms-2" do %>
                  <i class="fas fa-user"></i> <%= current_user.first_name %>
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <a href="<%= new_user_session_path %>" class="nav-link text-white">Se connecter</a>
              </li>
                <li class="nav-item">
                <a href="<%= new_user_registration_path %>" class="nav-link text-white">S'inscrire</a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container" style="margin-top: 100px;">
      <%= yield %>
    </div>

    <footer class="footer">
      <div class="bg-pink container-fluid d-flex justify-content-between">
        <p class="align-items-center align-content-center mt-2 mb-2" style="color: white;">Encore un projet</p>
        <div class="text-right align-items-center align-content-center mt-2 ml-3" style="color: white;">
          <span class="ms-4"><a href="<%=  %>" style="color: white; text-decoration: none;">Équipe</a></span>
          <span class="ms-4"><a href="<%=  %>" style="color: white; text-decoration: none;">Contact</a></span>
          <span class="ms-4"><a href="#" style="color: white; text-decoration: none;">Mentions légales</a></span>
        </div>
      </div>
    </footer>
  </body>
</html>
